{
  "name": "minume-room",
  "version": "1.0.0",
  "private": true,
  "description": "MINUME XVII - Plataforma académica full stack",
  "main": "index.js",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build:prod && npm --prefix backend install --omit=dev",
    "start": "node backend/server.js"
  }
}
